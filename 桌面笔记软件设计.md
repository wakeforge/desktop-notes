# 桌面笔记软件 · 详细设计与实施计划

> 目标：一个 Windows 桌面软件，可创建"笔记"，笔记能嵌入桌面、鼠标点击穿透（点不到），只能通过配套的配置软件来管理。

---

## 1. 项目概述

| 项 | 内容 |
|---|---|
| 平台 | Windows 10 / 11 |
| 技术栈 | Web 技术栈：UI 用 HTML/CSS/JS，宿主壳 **确定用 Electron**（自包含、API 全、坑少；未来考虑跨平台，Electron 天然支持 Win/Mac/Linux） |
| 核心特征 | 笔记窗口常驻桌面、透明、点击穿透、只能由配置窗口增删改 |
| 形态 | 系统托盘常驻 + 一个配置窗口 + 每笔记一个桌面挂件窗口 |

---

## 2. 为什么选 Web 技术栈

- 界面用 Web 写，开发快、样式漂亮、跨分辨率友好。
- Electron / WebView2 都原生暴露了做"桌面挂件"所需的能力：`transparent` 透明窗口、`setIgnoreMouseEvents` 点击穿透、`alwaysOnTop` 置顶、`screen` 多屏信息。
- 宿主壳只负责"开窗口 + 原生能力"，业务逻辑（笔记渲染、配置 UI）全在 Web 里，便于迭代。

> 决策：**采用 Electron**。它把 Chromium 打包进安装包（体积较大但开箱即用），API 最完整、坑最少，且**天然跨平台**（Windows / macOS / Linux），契合"未来考虑跨平台"的目标。业务 Web 代码与平台无关，只有"嵌入桌面/点击穿透"这类原生行为需按平台适配（Windows 用 `WorkerW`，macOS 用 `NSWindow` 层级，Linux 看桌面环境）。

---

## 3. 总体架构

```
┌─────────────────────────────────────────────┐
│               主进程 (Main)                    │
│  ┌──────────┐  ┌──────────┐  ┌────────────┐  │
│  │ 系统托盘  │  │ 窗口管理  │  │  存储层     │  │
│  │ Tray     │  │ WindowMgr│  │ notes.json │  │
│  └──────────┘  └──────────┘  └────────────┘  │
│        │              │                       │
│    ┌───┴────┐    ┌────┴─────┐                 │
│    │配置窗口 │    │ 笔记窗口×N │  (BrowserWindow) │
│    │Renderer│    │ Renderer │                 │
│    └────────┘    └──────────┘                 │
└─────────────────────────────────────────────┘
```

- **主进程（Node.js）**：创建/销毁窗口、托盘、调用原生 API（透明、点击穿透、置顶、多屏）、读写本地存储。
- **配置窗口（Renderer）**：普通窗口，管理笔记列表，编辑内容/位置/颜色/字号/透明度/锁定。技术可选原生 JS、Vue 或 React。
- **笔记窗口（Renderer）**：每笔记一个 `BrowserWindow`，无边框、透明、置顶；锁定态下只负责显示，不接受点击。
- **存储层**：本地 JSON 文件（MVP），进阶可换 SQLite。

---

## 4. 核心模块拆分

### 4.1 系统托盘与生命周期
- 托盘图标 + 右键菜单：`新建笔记` / `打开配置` / `全部锁定` / `退出`。
- 启动即常驻托盘，配置窗口默认隐藏（可随时从托盘唤出）。
- 退出时保存所有笔记状态。

### 4.2 配置窗口（管理 UI）
- 笔记列表（缩略信息：内容预览、所在屏幕、锁定状态）。
- 选中某笔记后可编辑：**富文本内容**（加粗/颜色/列表/链接/图片）、所在屏幕、X/Y 坐标、宽高、背景色、文字色、字号、透明度、是否置顶、是否锁定。
- 按钮：新建、删除、复制、应用并刷新桌面窗口。
- 全局设置：开机自启开关（默认可开可关）。

### 4.3 笔记窗口（桌面挂件）
- 每笔记一个独立 `BrowserWindow`。
- 关键窗口属性：
  - `frame: false` 无边框
  - `transparent: true` 透明背景
  - `alwaysOnTop: true` 置顶
  - `skipTaskbar: true` 不出现在任务栏
  - `resizable: false`（锁定态）
- 显示富文本内容 + 背景色/文字色/字号/透明度。
- **两态切换**：
  - **锁定态（默认）**：`setIgnoreMouseEvents(true, {forward:true})`，点击穿透、只读。
  - **解锁态（临时编辑）**：`setIgnoreMouseEvents(false)` + `resizable/movable` 打开，可直接在桌面拖动、缩放、编辑富文本；编辑完切回锁定态并保存。

### 4.4 临时解锁编辑（关键交互 ⭐）
- 触发：托盘菜单/配置窗口点"解锁"某笔记，或"全部解锁/全部锁定"。
- 解锁时该笔记恢复可交互（可点、可拖、可改），并显示轻量编辑工具条（富文本按钮）。
- 再次锁定：恢复点击穿透、写回 `notes.json`（含新位置/尺寸/内容）。
- 数据落点：位置/尺寸变化实时更新到内存，锁定或退出时持久化。

### 4.5 存储层
- 路径：`app.getPath('userData')/notes.json`。
- 负责：加载、保存、新增、更新、删除、启动时恢复所有笔记窗口。

### 4.6 点击穿透（关键技术 ⭐）
Electron 原生支持，无需 Win32 知识：
```js
// 锁定：让鼠标穿透到桌面
noteWin.setIgnoreMouseEvents(true, { forward: true });
// 解锁（如需临时可点）：
noteWin.setIgnoreMouseEvents(false);
```
`setIgnoreMouseEvents(true)` 后，该窗口不接收任何鼠标事件，点击会落到下面的桌面 —— 正是你要的"点不到"。

### 4.7 "嵌入桌面"的两种层级（关键技术 ⭐）
笔记在桌面上存在两种形态，MVP 用第一种即可满足"点不到 + 只能软件配"：

- **A. 置顶透明层（推荐首发）**
  `alwaysOnTop: true`（可指定 `'floating'` 等层级）。笔记浮在桌面之上、其它窗口之下/之上，但因为 `setIgnoreMouseEvents(true)`，你点不到它，桌面照常可用。实现最简单、纯 JS 即可。
- **B. 真·桌面子窗口（进阶）**
  把笔记窗口 `SetParent` 挂到桌面宿主 `WorkerW` 下，让它像桌面图标一样处于最底层、被普通窗口遮挡。Electron 纯 JS 不原生支持 `SetParent`，需用 **C++ 原生插件（Node-API addon）** 或 `ffi` 调用 `user32.dll` 的 `SetParent` / `SetWindowLong`。属于 M3 进阶项。

---

## 5. 数据格式（notes.json）

```json
{
  "version": 1,
  "settings": {
    "launchOnStartup": true,
    "defaultBgColor": "#FFF7B2",
    "defaultTextColor": "#222222",
    "defaultFontSize": 14
  },
  "notes": [
    {
      "id": "a1b2c3d4",
      "contentHtml": "<p><b>购物</b></p><ul><li>买菜</li><li>取快递</li></ul>",
      "displayId": 2779098405,
      "x": 120, "y": 240,
      "width": 240, "height": 160,
      "bgColor": "#FFF7B2",
      "textColor": "#222222",
      "fontSize": 14,
      "opacity": 0.9,
      "locked": true,
      "alwaysOnTop": true,
      "createdAt": "2026-07-21T20:00:00+08:00",
      "updatedAt": "2026-07-21T20:00:00+08:00"
    }
  ]
}
```

字段说明：
- `contentHtml`：富文本内容（受控 HTML，渲染前做净化，防 XSS）。
- `displayId`：Electron `screen` 模块返回的显示器 id；`x/y` 为该屏工作区内的相对坐标，换屏/分辨率变化时按屏还原。
- `locked`：是否锁定（穿透只读）；`launchOnStartup`：开机自启开关。

---

## 6. 实施里程碑（已按最终范围重排）

> 已确认范围：多屏、富文本、开机自启（可关）、临时解锁编辑 —— **全部纳入 MVP(M1)**；云同步不做；宿主壳用 Electron，未来考虑跨平台。

### M1 — 最小可用原型（本次交付目标，可运行闭环）
- 托盘 + 配置窗口 + 新建/删除/复制/编辑笔记。
- 每个笔记一个透明置顶窗口（`frame:false`、`transparent:true`、`alwaysOnTop:true`、`skipTaskbar:true`）。
- **点击穿透**：锁定态 `setIgnoreMouseEvents(true,{forward:true})`。
- **临时解锁编辑**：解锁后可拖动/缩放/改内容，锁定后恢复穿透并保存。
- **富文本**：`contenteditable` + 净化，支持加粗/颜色/列表/链接（图片可作 M1.5）。
- **多显示器**：`screen` 模块按 `displayId` + 屏内相对坐标摆放与还原。
- **开机自启**：`app.setLoginItemSettings`，设置里可开可关。
- JSON 存储 + 启动时恢复所有笔记。
- ✅ 交付：能实际跑起来、多屏正确摆放、看到富文本桌面笔记、锁定态点不到、可临时解锁编辑、开机自启可配。

### M2 — 体验完善
- 富文本插入图片 / 更丰富的样式面板。
- 配置窗口内拖拽预览定位（可视化摆位缩略图）。
- 笔记模板、标签、置顶层级细分。
- 打包与代码签名、自动更新。

### M3 — 进阶（可选）
- 真·嵌入桌面（`WorkerW` `SetParent` 原生模块，实现 B 方案，笔记沉到桌面图标层）。
- **跨平台适配**：macOS（`NSWindow` 层级 / `setIgnoreMouseEvents` 已跨平台）、Linux。
- 云同步（账号体系 + 后端，视需要再启动）。

---

## 7. 风险与权衡

| 项 | 说明 |
|---|---|
| 置顶 vs 真桌面层 | MVP 的置顶方案最稳；真桌面层需原生插件，复杂度高。 |
| 多显示器 | 需处理各屏 `bounds`，否则笔记会跑错屏。 |
| 透明窗口性能 | 笔记数量多时建议限制 DOM 复杂度。 |
| 杀软/打包 | Electron 打包后体积大，个别杀软可能误报，需代码签名。 |

---

## 8. 已确认的最终范围（决策记录）

| # | 细节 | 决策 | 说明 |
|---|---|---|---|
| 1 | 多显示器 | ✅ 进 MVP | 数据层用 `displayId` + 屏内相对坐标，换屏可正确还原 |
| 2 | 文本类型 | ✅ 富文本 | 加粗/颜色/列表/链接（图片可 M1.5/M2），内容存净化后的 HTML |
| 3 | 开机自启 / 云同步 | ✅ 自启（可关） / ❌ 同步暂不做 | 自启 `setLoginItemSettings`，设置里开关；同步留 M3 |
| 4 | 临时解锁编辑 | ✅ 要 | 锁定态穿透只读，解锁态可拖/缩放/改，改完锁回 |
| 5 | 宿主壳 | ✅ Electron | 未来考虑跨平台，Electron 天然支持 Win/Mac/Linux |

> 范围已锁定。下一步：按 M1 里程碑搭建 Electron 工程并实现可运行版本。
